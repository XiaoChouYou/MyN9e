

version: '3.0'

networks:
  net_n9e:
    driver: bridge
    ipam:
      config:
        - subnet: ${SUBNETWORK}

services:
  dns-server:
    container_name: n9e-dns
    image: 'jpillora/dnsmasq'
    restart: always
    networks:
      net_n9e:
        ipv4_address: ${DNSSERVICE}
    environment:
      - TZ=Asia/Shanghai
      - HTTP_USER=${DNS_USER}
      - HTTP_PASS=${DNS_PASSWD}
    ports:
      - 53:53/udp
      - 8080:8080
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${PROJECTHOME}/dns/resolv.conf:/etc/resolv.conf:ro
      - ${PROJECTHOME}/dns/dnsmasq.conf:/etc/dnsmasq.conf:rw


  timescaledb:
    image: xiaochouyou/promscale-extension:0.6.0-ts2.7.2-pg14
    container_name: n9e-timescaledb
    restart: always
    networks:
      net_n9e:
        ipv4_address: ${POSTGRES_SERVICE}
    environment:
      - csynchronous_commit=off
      - POSTGRES_PASSWORD=${POSTGRES_PASSWD}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_DB=${POSTGRES_DB}
    ports:
      - 5432:5432
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${PROJECTHOME}/dns/resolv.conf:/etc/resolv.conf:ro
      - ${PROJECTHOME}/timescaledb/data:/var/lib/postgresql/data:rw
    mem_limit: 512m
    depends_on:
      - dns-server


  promscale:
    image: timescale/promscale:0.14
    container_name: n9e-promscale
    restart: always
    networks:
      net_n9e:
        ipv4_address: ${PROMSCALE_SERVICE}
    environment:
      - PROMSCALE_DB_HOST=${PROMSCALE_DB_HOST}
      - PROMSCALE_DB_PORT=${PROMSCALE_DB_PORT}
      - PROMSCALE_DB_SSL_MODE=allow
      - PROMSCALE_DB_NAME=${PROMSCALE_DB_NAME}
      - PROMSCALE_DB_PASSWORD=${PROMSCALE_DB_PASSWORD}
      - WAIT_HOSTS=dns-server:8080,timescaledb:5432
    ports:
      - 9201:9201
      - 9202:9202
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${PROJECTHOME}/dns/resolv.conf:/etc/resolv.conf:ro
    mem_limit: 512m
    depends_on:
      - dns-server
      - timescaledb


  prometheus:
    image: prom/prometheus:v2.33.4
    container_name: n9e-prometheus
    restart: always
    networks:
      net_n9e:
        ipv4_address: ${PROMETHEUS_SERVICE}
    ports:
      - 9090:9090
      - 9093:9093
      - 9100:9100
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${PROJECTHOME}/dns/resolv.conf:/etc/resolv.conf:ro
      - ${PROJECTHOME}/prometheus/data:/prometheus:rw
      - ${PROJECTHOME}/prometheus/etc:/etc/prometheus:rw
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention=45d'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--query.lookback-delta=2m'
      - '--enable-feature=remote-write-receiver'
      - '--web.enable-lifecycle'
      - '--web.enable-admin-api'
    mem_limit: 512m
    stop_grace_period: 10s
    depends_on:
      - promscale







#  nginx:
#    container_name: n9e-nginx
#    image: nginx
#    restart: always
#    networks:
#      net_n9e:
#        ipv4_address: ${NGINXSERVICE}
#    environment:
#      - NGINX_PORT=10080
#    ports:
#      - 10080:10080
#    volumes:
#      - /etc/localtime:/etc/localtime:ro
#      - ${PROJECTHOME}/dns/resolv.conf:/etc/resolv.conf:ro
#      - ${PROJECTHOME}/nginx/etc:/etc/nginx:rw
#      - ${PROJECTHOME}/nginx/html:/usr/share/nginx/html:rw
#      - ${PROJECTHOME}/nginx/logs:/var/log/nginx:rw
#    mem_limit: 512m
#    depends_on:
#      - dns-server
#
