# # collect interval
# interval = 15

[[instances]]
cluster_name = "zk-cluster"
labels = { instance="zk-cluster-1" }
addresses =  "192.168.0.130:22181"

[[instances]]
cluster_name = "zk-cluster"
labels = { instance="zk-cluster-2" }
addresses =  "192.168.0.130:22182"

[[instances]]
cluster_name = "zk-cluster"
labels = { instance="zk-cluster-3" }
addresses =  "192.168.0.130:22183"


# cluster_name = "dev-zk-cluster"
# addresses = "127.0.0.1:2181"
# timeout = 10

# important! use global unique string to specify instance
# labels = { instance="n9e-10.2.3.4:2181" }

## Optional TLS Config
# use_tls = false
# tls_min_version = "1.2"
# tls_ca = "/etc/categraf/ca.pem"
# tls_cert = "/etc/categraf/cert.pem"
# tls_key = "/etc/categraf/key.pem"
## Use TLS but skip chain & host verification
# insecure_skip_verify = true
